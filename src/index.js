/**
 * Function to add two variables
 * @param {NUMBER} a
 * @param {NUMBER} b
 * @returns {NUMBER}
 */
const sum = (a, b) => {
  return a + b;
}

console.log(sum(1,10)); //output 11
console.log(sum(10,30)); //output 40